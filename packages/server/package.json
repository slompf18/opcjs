{
  "name": "opcjs-server",
  "version": "0.2.0-alpha",
  "description": "OPC UA server library for Node.js.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/iarbre/opcjs.git"
  },
  "type": "module",
  "sideEffects": false,
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "require": "./dist/index.cjs"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "lint": "eslint ./src",
    "tsc": "npx tsc --noEmit",
    "tsc:test": "npx tsc -p tsconfig.test.json --noEmit",
    "build": "tsup",
    "verify": "npm run lint && npm run tsc && npm run tsc:test && npm run build && npm run test:unit",
    "dev": "tsup --watch",
    "test": "vitest run",
    "test:unit": "vitest run --config vitest.unit.config.ts",
    "test:int": "vitest run tests/integration",
    "test:dev": "vitest"
  },
  "keywords": [
    "opcua",
    "opc-ua",
    "server"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "opcjs-base": "*",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^22.0.0",
    "@types/ws": "^8.5.13",
    "nx": "^22.6.4",
    "tsup": "^8.3.5",
    "typescript": "^6.0.2",
    "typescript-eslint": "^8.58.0",
    "vitest": "^4.0.18"
  }
}
